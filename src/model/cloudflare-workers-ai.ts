/** Classify an unknown Workers AI failure into a safe model reason. */
export {
  cloudflareWorkersAiClassifyError as classifyError,
  cloudflareWorkersAiErrorCode as errorCode,
} from "../adapters/cloudflare-workers-ai-errors.js"
